% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjustment-exclusions.R
\name{exclude_adjustment_vars}
\alias{exclude_adjustment_vars}
\title{Remove excluded adjustment variables from an adjustment matrix}
\usage{
exclude_adjustment_vars(W, name, adjustment_exclusions, verbose = FALSE)
}
\arguments{
\item{W}{Adjustment matrix (data frame) for the current candidate variable.}

\item{name}{Name of the candidate variable whose importance is being
estimated.}

\item{adjustment_exclusions}{Named list as passed to \code{\link{varimpact}}.}

\item{verbose}{If TRUE, report which columns were dropped.}
}
\value{
\code{W} without the excluded columns, unchanged when nothing was
  requested for \code{name} or nothing matched.
}
\description{
Drops the adjustment variables the user excluded for this candidate
variable, resolving each requested name to every column it contributed via
\code{\link{adjustment_columns}}.
}
\details{
This runs inside the per-variable, per-fold loop, so it stays silent: the
spelling of \code{adjustment_exclusions} is checked once up front by
\code{\link{check_adjustment_exclusions}} rather than warning on every fold.
}
\seealso{
\code{\link{varimpact}}
}
